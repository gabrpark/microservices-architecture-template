# version: '3'
# services:
#   flaskapp:
#     build: .
#     container_name: flaskapp
#     env_file:
#       - .env
#     expose:
#       - "8000"
#   nginx:
#     image: nginx:latest
#     container_name: nginx
#     ports:
#       - "80:80"
#     volumes:
#       - ./nginx.conf:/etc/nginx/conf.d/default.conf
#     depends_on:
#       - flaskapp

version: '3'
services:
  adaptive_text:
    build: ./adaptive_text
    ports:
      - "5002:5002"
    # environment:
    #   - MONGO_URI=mongodb://mongo:27017
    environment:
      - LOCAL_MONGO_URI=mongodb://host.docker.internal:27017
      - MONGO_DB_NAME=adaptiveTextDB
    extra_hosts:
      - "host.docker.internal:host-gateway"
    # depends_on:
    #   - mongo

  clasp_mobile:
    build: ./clasp_mobile
    ports:
      - "5001:5001"
    environment:
      - LOCAL_MONGO_URI=mongodb://host.docker.internal:27017
      - MONGO_DB_NAME=claspMobileDB
    extra_hosts:
      - "host.docker.internal:host-gateway"
    #   depends_on:
    #     - mongo

    #   mongo:
    #     image: mongo
    #     volumes:
    #       - mongo-data:/data/db

    # volumes:
    #   mongo-data:
